<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.cqray.android">

    <application>
        <!-- 无痛初始化 -->
        <provider
            android:name="._Get"
            android:authorities="${applicationId}.get"
            android:exported="false" />
    </application>
</manifest>
